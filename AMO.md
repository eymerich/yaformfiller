# AMO listing

## Summary (max 135 chars)

Automatically fills form fields on matching pages using saved forms (field type, CSS selector, value), via an on-page topbar.

## Description (max 250 chars)

YaFormFiller saves reusable forms — field type, jQuery-style CSS selector and value — and fills them automatically on matching domains. Manage forms, valid domains and fields in Options, with import/export of data. No data collected.
